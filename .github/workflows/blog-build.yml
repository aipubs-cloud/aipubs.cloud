name: Build Blog

on:
  push:
    paths:
      - 'blog/**'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate blog structure
        run: test -d blog/incoming && test -d blog/published
